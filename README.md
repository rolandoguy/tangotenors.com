# Tango Tenors

Minimal first version of the official Tango Tenors website.

## Local preview

Open `index.html` directly, or serve this folder with any static web server.

## Deployment

This is a static site prepared for GitHub → Cloudflare Pages. The ticket link is a clearly marked placeholder in `index.html` and should be replaced before launch.
